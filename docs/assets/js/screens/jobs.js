import {currentCrew,state} from '../store.js';
import {esc,formatDate} from '../ui.js';
export function jobs(){const c=currentCrew(),tasks=state.data.tasks.filter(t=>t.crew_id===c.crew_id&&t.status!=='archived');return `<header class="top"><span class="brand">JOBS</span><button class="secondary" data-add-task>Add job</button></header><h1>${esc(c.display_name)}’s jobs</h1><div class="list">${tasks.length?tasks.map(t=>`<button class="list-row" data-task="${t.task_id}"><span class="task-dot"></span><span class="grow"><b>${esc(t.title)}</b><span class="small">${formatDate(t.due_date)} · ${esc(t.status)}</span></span><span>›</span></button>`).join(''):'<p class="empty">No jobs yet. Add the first one.</p>'}</div>`}
